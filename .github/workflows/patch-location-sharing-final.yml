name: Apply Yamone location sharing final hardening

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-location-sharing-final.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply location sharing final fixes
        run: python3 .github/scripts/patch_location_sharing_final.py
      - name: Commit changes
        run: |
          git config user.name 'Yamone Build Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add app/src/main/java/com/yamo/snorelab
          git commit -m 'Finalize location sharing retries and alerts'
          git push
