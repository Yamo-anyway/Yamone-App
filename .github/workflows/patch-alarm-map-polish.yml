name: Apply Yamone alarm and map polish

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/patch-alarm-map-polish.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply alarm and map polish
        run: python3 .github/scripts/patch_alarm_map_polish.py
      - name: Commit changes
        run: |
          git config user.name 'Yamone Build Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add app/src/main/java/com/yamo/snorelab
          git commit -m 'Polish ringing alarm and location map UI'
          git push
